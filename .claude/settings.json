{
  "sandbox": {
    "excludedCommands": ["make", "vusted"]
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "cmd=$(jq -r '.tool_input.command'); echo \"$cmd\" | grep -q '^git commit' || exit 0; make test || echo '{\"continue\": false, \"stopReason\": \"make test failed\"}'",
            "timeout": 120,
            "statusMessage": "Running make test..."
          }
        ]
      }
    ]
  }
}
